'use client';

import { FC } from 'react';


export const WtcProvider: FC<{ children: React.ReactNode }> = ({ children }) => {
    

    return (
        <div>
            {children}
        </div>
    );
};
